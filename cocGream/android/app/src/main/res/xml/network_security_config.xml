<?xml version="1.0" encoding="utf-8"?>
<!--
  Allow cleartext HTTP only to the CockroachGram backend (and dev loopbacks).
  Remove the production block once the API is fronted by TLS (Caddy / nginx + Let's Encrypt).
-->
<network-security-config>

  <!-- Live backend on bare-IP HTTP. Strip this block when HTTPS is live. -->
  <domain-config cleartextTrafficPermitted="true">
    <domain includeSubdomains="false">155.248.250.88</domain>
  </domain-config>

  <!-- Dev loopbacks: emulator (10.0.2.2) + adb-reverse'd localhost. -->
  <domain-config cleartextTrafficPermitted="true">
    <domain includeSubdomains="false">10.0.2.2</domain>
    <domain includeSubdomains="false">localhost</domain>
    <domain includeSubdomains="false">127.0.0.1</domain>
  </domain-config>

  <!-- Default: everything else stays HTTPS-only. -->
  <base-config cleartextTrafficPermitted="false">
    <trust-anchors>
      <certificates src="system" />
    </trust-anchors>
  </base-config>

</network-security-config>
